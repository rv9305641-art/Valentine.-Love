<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Will You Be My Valentine? 💖</title>

<style>
*{box-sizing:border-box;font-family:'Poppins','Segoe UI',sans-serif}

body{
margin:0;
min-height:100vh;
display:flex;
justify-content:center;
align-items:center;
background:linear-gradient(135deg,#ff9a9e,#fad0c4);
overflow:hidden;
}

.card{
background:#fff0f6;
border-radius:24px;
padding:30px 22px 38px;
text-align:center;
max-width:420px;
width:92%;
box-shadow:0 10px 30px rgba(255,105,180,.35);
animation:float 3s ease-in-out infinite;
}

@keyframes float{
0%,100%{transform:translateY(0)}
50%{transform:translateY(-6px)}
}

h1{color:#ff2f92;margin-bottom:10px;font-size:1.9rem}
p{color:#d63384;margin-top:0;font-size:1.05rem}
.hearts{font-size:1.6rem;margin-bottom:10px}

.countdown{
margin-top:10px;
font-size:1.05rem;
font-weight:600;
color:#ff2f92;
}

.buttons{
margin-top:26px;
position:relative;
width:100%;
height:90px;
display:flex;
justify-content:center;
align-items:center;
gap:20px;
}

button{
border:none;
border-radius:999px;
padding:14px 28px;
font-size:1.05rem;
font-weight:600;
cursor:pointer;
transition:.15s;
}

#yesBtn{
background:linear-gradient(135deg,#ff5fa2,#ff8fc7);
color:#fff;
box-shadow:0 6px 14px rgba(255,47,146,.45);
z-index:2;
}

#yesBtn:hover{transform:scale(1.08)}

#noBtn{
background:#ffe1ec;
color:#ff2f92;
position:relative;
z-index:1;
}

.result{
display:none;
margin-top:22px;
color:#ff2f92;
font-size:1.2rem;
font-weight:600;
animation:pop .5s ease forwards;
}

@keyframes pop{
0%{transform:scale(.6);opacity:0}
100%{transform:scale(1);opacity:1}
}

.heart{
position:fixed;
bottom:-20px;
font-size:18px;
animation:rise linear forwards;
pointer-events:none;
}

@keyframes rise{
to{transform:translateY(-110vh) rotate(360deg);opacity:0}
}

.firework{
position:fixed;
width:6px;
height:6px;
background:#ff2f92;
border-radius:50%;
animation:explode 1s ease-out forwards;
}

@keyframes explode{
to{transform:scale(15);opacity:0}
}

.confetti{
position:fixed;
width:8px;
height:8px;
animation:fall 3s linear forwards;
}

@keyframes fall{
to{transform:translateY(110vh) rotate(720deg);opacity:0}
}
</style>
</head>

<body>

<audio id="bgMusic" loop>
<source src="https://assets.mixkit.co/music/preview/mixkit-romantic-piano-melody-105.mp3" type="audio/mpeg">
</audio>

<div class="card">
<div class="hearts">💗 💕 💖 💞 💓</div>

<h1>Will You Be My Valentine, Lovely? 🌸</h1>
<p>Lovely, you make my world brighter, sweeter, and full of love. Will you be mine this Valentine’s Day? 💕</p>

<div class="countdown" id="countdown"></div>

<div class="buttons">
<button id="yesBtn">Yes 💖</button>
<button id="noBtn">No 🙈</button>
</div>

<div id="result" class="result">Yaaay!! I knew you'd say YES! 💞🥰🌷</div>
</div>

<script>
const noBtn=document.getElementById("noBtn");
const yesBtn=document.getElementById("yesBtn");
const result=document.getElementById("result");
const container=document.querySelector(".buttons");
const music=document.getElementById("bgMusic");

function moveNo(){
const maxX=container.offsetWidth-noBtn.offsetWidth;
const maxY=container.offsetHeight-noBtn.offsetHeight;
const x=Math.random()*maxX;
const y=Math.random()*maxY;
noBtn.style.position="absolute";
noBtn.style.left=x+"px";
noBtn.style.top=y+"px";
}

noBtn.addEventListener("mouseenter",moveNo);
noBtn.addEventListener("click",moveNo);

yesBtn.addEventListener("click",()=>{
result.style.display="block";
music.play();
hearts();
confetti();
fireworks();
});

function hearts(){
for(let i=0;i<50;i++){
setTimeout(()=>{
const h=document.createElement("div");
h.className="heart";
h.textContent=["💖","💗","💕","💞","💓"][Math.floor(Math.random()*5)];
h.style.left=Math.random()*100+"vw";
h.style.animationDuration=3+Math.random()*3+"s";
document.body.appendChild(h);
setTimeout(()=>h.remove(),6000);
},i*70);
}
}

function fireworks(){
for(let i=0;i<25;i++){
const f=document.createElement("div");
f.className="firework";
f.style.left=Math.random()*100+"vw";
f.style.top=Math.random()*100+"vh";
document.body.appendChild(f);
setTimeout(()=>f.remove(),1000);
}
}

function confetti(){
for(let i=0;i<70;i++){
const c=document.createElement("div");
c.className="confetti";
c.style.left=Math.random()*100+"vw";
c.style.background=["#ff2f92","#ffd1e6","#ff85b3","#ffb6d5"][Math.floor(Math.random()*4)];
c.style.animationDuration=2+Math.random()*3+"s";
document.body.appendChild(c);
setTimeout(()=>c.remove(),5000);
}
}

const countdown=document.getElementById("countdown");
const valentine=new Date(new Date().getFullYear(),1,14);

function updateCountdown(){
const now=new Date();
const diff=valentine-now;
if(diff<=0){countdown.textContent="💘 Happy Valentine's Day! 💘";return;}
const d=Math.floor(diff/(1000*60*60*24));
const h=Math.floor((diff/(1000*60*60))%24);
const m=Math.floor((diff/(1000*60))%60);
countdown.textContent=`⏳ ${d} Days ${h} Hours ${m} Minutes left`;
}
setInterval(updateCountdown,1000);
updateCountdown();
</script>

</body>
</html>
